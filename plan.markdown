### Переработанный план разработки QML-приложения Task Tracker (без изменений в C++ бэкенде)

**Цель**: Обновить пользовательский интерфейс приложения Task Tracker, сохранив текущую функциональность (добавление, редактирование, удаление задач) и существующую C++ часть (`Task`, `TaskModel`, `TaskRepository`), но сделав интерфейс современным, минималистичным, интуитивно понятным и готовым к будущим обновлениям. План фокусируется на QML, учитывает текущую вертикальную компоновку, адаптивность и возможности расширения (например, категории, сроки, фильтры) без модификации бэкенда.

---

## Этапы разработки

### Этап 1: Подготовка и реорганизация QML-структуры
**Цель**: Создать модульную структуру и подготовить проект к изменениям, сохранив совместимость с текущим бэкендом.

1. **Реорганизация директорий**:
   - Перенести `Header.qml`, `AddTaskForm.qml`, `TaskItem.qml` в папку `components/ui`.
   - Создать папку `components/styles` для стилей и тем (например, `Style.qml`).
   - Создать папку `components/controls` для кастомных компонентов (например, кнопки, карточки).

2. **Централизация стилей**:
   - Создать `Style.qml` с константами:
     - **Цвета**: Основной фон (`#F5F5F5` — светлая, `#1C2526` — темная), акцентный цвет (`#007AFF`), приоритеты (зеленый `#34C759`, оранжевый `#FF9500`, красный `#FF3B30`).
     - **Шрифты**: Размеры (24px — заголовки, 16px — текст, 12px — второстепенный), отступы (8px, 16px).
     - **Темы**: Поддержка светлой/темной темы через свойства (`property color`) или `palette`.
   - Настроить переключение тем через переменную в `main.qml`.

3. **Подключение шрифтов**:
   - Использовать `FontLoader` для современного шрифта (например, `Roboto` или `Inter`).
   - Настроить локализацию через `qsTr()` для будущей многоязычности.

4. **Подготовка к расширяемости**:
   - Создать задел для фильтров и категорий, используя `ListView` с `delegate` и `SortFilterProxyModel` (без реализации фильтров в бэкенде, только QML-обертка).
   - Добавить поддержку динамической загрузки компонентов через `Loader`.

**Срок**: 1–2 дня.

---

### Этап 2: Обновление заголовка (`Header.qml`)
**Цель**: Сделать заголовок минималистичным и готовым к новым функциям.

1. **Дизайн**:
   - Убрать фоновый цвет `lightblue`, заменив на прозрачный или легкий градиент (`LinearGradient`).
   - Добавить иконку меню ("гамбургер") слева с использованием `FontAwesome` или `Material Icons` через `FontLoader`.
   - Сохранить центрированный текст "Task Tracker" с размером шрифта 24px, но использовать шрифт из `Style.qml`.

2. **Интерактивность**:
   - Добавить сигнал для открытия будущей боковой панели (`Drawer`) для настроек (например, темы, фильтры).
   - Использовать `MouseArea` для обработки клика по иконке меню.

3. **Анимации**:
   - Анимация нажатия на иконку меню (`ScaleAnimator`, масштабирование на 0.9 при клике).
   - Плавное изменение цвета фона при смене темы (`ColorAnimation`).

4. **Расширяемость**:
   - Сделать текст заголовка динамическим (например, для отображения категории задач через свойство `text`).
   - Подготовить место для будущих элементов (например, кнопка поиска).

**Срок**: 1 день.

---

### Этап 3: Обновление формы задачи (`AddTaskForm.qml`)
**Цель**: Создать современную, интуитивную форму с заделом на новые поля.

1. **Переход на `Popup`**:
   - Заменить `Item` на `Popup` с `modal: true` для мобильных устройств, центрированный на экране.
   - Для десктопа предусмотреть возможность использования `Drawer` (боковой выезд).

2. **Обновление полей**:
   - Заменить `TextField` для описания на `TextArea` для многострочного ввода.
   - Заменить `ComboBox` для приоритета на кастомный компонент с тремя кнопками (`Row` с `Button` или `RadioButton`) для интуитивного выбора (Низкий, Средний, Высокий).
   - Добавить пустое поле `TextField` для будущих категорий (неактивное, но с заделом на активацию).
   - Добавить пустое поле для срока выполнения (например, `TextField` с `placeholderText: "Срок выполнения"`, пока без функциональности).

3. **Кнопки**:
   - Стилизовать "Сохранить" акцентным цветом (`#007AFF`), "Отмена" — серым (`#E0E0E0`).
   - Добавить валидацию: проверка непустого названия задачи через `if (nameField.text.trim() !== "")`.
   - Использовать стили из `Style.qml` для единообразия.

4. **Анимации**:
   - Плавное появление формы снизу (`YAnimator`) или с увеличением прозрачности (`OpacityAnimator`).
   - Анимация нажатия кнопок (`ScaleAnimator`, масштабирование на 0.95).

5. **Расширяемость**:
   - Добавить сигналы для будущих полей (`category`, `dueDate`), даже если они пока не используются бэкендом.
   - Использовать `Loader` для динамического добавления новых полей (например, теги, напоминания).

**Срок**: 2–3 дня.

---

### Этап 4: Обновление списка задач и элементов (`TaskItem.qml`)
**Цель**: Создать карточный дизайн с улучшенной интерактивностью и заделом на новые функции.

1. **Дизайн карточек**:
   - Сделать каждую задачу карточкой с легкой тенью (`BoxShadow` или `elevation` через `Rectangle`) и радиусом углов (`radius: 8`).
   - Компактное отображение: название и приоритет в одной строке, описание скрыто (разворачивается при клике через `MouseArea`).
   - Убрать чередование фона (`lightgray/white`), заменив на единый цвет (`#FFFFFF` или темный эквивалент).
   - Добавить иконки для приоритета (например, круглые маркеры с цветами из `Style.qml`).

2. **Интерактивность**:
   - Использовать `SwipeDelegate` для свайпа влево (удаление) и вправо (будущий статус "завершено").
   - Добавить `CheckBox` для отметки завершенных задач (пока без функциональности, но с сигналом для будущего).
   - Добавить долгое нажатие (`MouseArea` с `onPressAndHold`) для открытия контекстного меню (`Popup` с опциями: редактировать, удалить).

3. **Анимации**:
   - Анимация появления карточки при добавлении (`OpacityAnimator`).
   - Анимация удаления карточки (сдвиг влево через `XAnimator` или уменьшение прозрачности).
   - Эффект нажатия на карточку (`ScaleAnimator`, масштабирование на 0.95).

4. **Расширяемость**:
   - Добавить пустое место для отображения категории и срока (например, `Text` с `visible: false`).
   - Подготовить `ListView` к фильтрации через `SortFilterProxyModel` (например, по приоритету).

**Срок**: 2–3 дня.

---

### Этап 5: Обновление главного окна (`main.qml`)
**Цель**: Интегрировать обновленные компоненты и добавить элементы для будущих функций.

1. **Плавающая кнопка (FAB)**:
   - Заменить кнопку "Добавить задачу" на `FloatingActionButton` в правом нижнем углу (например, `Rectangle` с `radius: 28` и иконкой "+").
   - Добавить тень (`BoxShadow`) и анимацию нажатия (`ScaleAnimator`).

2. **Панель фильтров/поиска**:
   - Добавить `TextField` для поиска задач (пока без функциональности, с заделом на `SortFilterProxyModel`).
   - Добавить кнопки фильтрации (например, `Row` с иконками для приоритета) с неактивным состоянием.

3. **Боковая панель (`Drawer`)**:
   - Добавить `Drawer` для настроек (переключение тем, выбор языка).
   - Реализовать переключение светлой/темной темы через свойство в `Style.qml` и `palette`.

4. **Адаптивность**:
   - Использовать `Screen` из `QtQuick.Window` для динамической адаптации отступов и размеров шрифта.
   - На мобильных устройствах форма задачи открывается как `Popup`, на десктопе — как `Drawer`.
   - На планшетах предусмотреть задел для двухколоночного режима (`SplitView`).

5. **Уведомления**:
   - Добавить `Popup` или кастомный `SnackBar` для уведомлений (например, "Задача добавлена").
   - Использовать `Timer` для автоматического скрытия уведомлений через 2–3 секунды.

6. **Анимации**:
   - Плавная смена темы (`ColorAnimation` для фона и текста).
   - Плавная прокрутка `ListView` с эффектом "подпружинивания" (`boundsBehavior: Flickable.StopAtBounds`).

**Срок**: 2–3 дня.

---

### Этап 6: Оптимизация и тестирование
**Цель**: Обеспечить производительность и стабильность интерфейса.

1. **Оптимизация**:
   - Настроить `cacheBuffer` в `ListView` для плавной прокрутки.
   - Использовать `Loader` для ленивой загрузки компонентов (например, формы задачи).

2. **Тестирование**:
   - Проверить адаптивность на разных разрешениях (мобильные, планшеты, десктоп).
   - Проверить анимации на низкопроизводительных устройствах.
   - Убедиться, что текущая функциональность (добавление, редактирование, удаление) работает без ошибок.
   - Проверить работу локализации (`qsTr()`) с тестовым переводом.

3. **Расширяемость**:
   - Проверить, что новые поля (категория, срок) не ломают текущую логику.
   - Убедиться, что `SortFilterProxyModel` готов к активации фильтров.

**Срок**: 2 дня.

---

## Общий срок
- **Итого**: 10–14 дней (в зависимости от сложности анимаций и тестирования).

## Рекомендации
- Использовать `QtQuick.Controls 2` для всех компонентов (например, `Button`, `TextField`, `Popup`).
- Тестировать на реальных устройствах (Android, Windows) для проверки адаптивности.
- Документировать новые компоненты и сигналы в QML для упрощения будущих доработок.
- Сохранить совместимость с текущим `TaskModel` и `TaskRepository`, добавляя новые поля как неактивные в UI.
- Использовать `Style.qml` для централизованного управления цветами, шрифтами и отступами.

---

**Примечание**: План ориентирован на редизайн интерфейса с минимальными изменениями в логике, сохраняя текущий функционал и добавляя задел для будущих функций (категории, сроки, фильтры) без модификации C++ бэкенда. Если нужны конкретные QML-файлы или примеры кода для отдельных компонентов, напиши, и я начну реализацию!

Жду дальнейших указаний.







### Ключевые моменты
- Приложение представляет собой мобильный таск-трекер с нижней навигацией и основными разделами: панель задач, список покупок, кнопка добавления и настройки.  
- Панель задач — главный экран с фильтрами, списком задач и возможностью взаимодействия (нажатие, свайп).  
- Другие экраны, такие как список покупок и настройки, пока имеют базовую функциональность с заделом на будущее.  

---

### Общее описание
**Введение**  
Приложение TaskTracker — это мобильный таск-трекер, ориентированный на простоту и удобство управления повседневными задачами. Интерфейс минималистичный, с акцентом на визуальную иерархию и быстрый доступ к основным функциям. Навигация осуществляется через нижнюю панель с четырьмя основными разделами: панель задач, список покупок, кнопка добавления и настройки.  

**Панель задач**  
Это основной экран, где пользователи взаимодействуют с задачами. Он включает верхнюю панель фильтров для сортировки задач и список задач, представленный в виде карточек. Каждая карточка содержит название, дедлайн и визуальный индикатор приоритета (зелёный — низкий, оранжевый — средний, красный — высокий). Пользователи могут нажимать на задачи для редактирования, свайпать для быстрого удаления или отметки как выполненной, а также использовать фильтры для упрощения навигации.  

**Другие экраны**  
Список покупок пока представляет собой заглушку с сообщением "Скоро", а настройки позволяют ввести имя пользователя с заделом на будущие функции, такие как выбор темы или статистика.  

---

---

### Подробное описание функционала QML-приложения TaskTracker

#### Общая концепция и структура
TaskTracker — это мобильное приложение для управления задачами, ориентированное на интуитивность и минимализм. Интерфейс построен вокруг нижней навигационной панели, обеспечивающей доступ к основным разделам: панель задач (основной экран), список покупок, кнопка добавления и настройки. Дизайн использует нейтральную цветовую палитру (светло-серый фон, белые карточки) с акцентами на приоритетах (зелёный, оранжевый, красный) и читаемые шрифты без засечек, такие как Inter или SF Pro. Анимации (появление, свайп) добавляют плавности взаимодействию.

Цель — создать интерфейс, где приоритет задач сразу виден по цвету, управление осуществляется минимальным количеством кликов, а есть защита от случайных действий, например, возможность отмены удаления задачи.

#### Навигационная панель (Bottom Navigation)
- **Назначение**: Обеспечивает быстрый доступ к основным разделам приложения.  
- **Визуальное состояние**:  
  - Четыре пункта: "Задачи", "Покупки", "+", "Настройки".  
  - Активный пункт выделен (например, подчёркиванием или цветом акцента, например, синий #007AFF).  
  - Кнопка "+" — круглая, плавающая (FAB), центрирована, с акцентным цветом.  
- **Логика работы**:  
  - При тапе на пункт переключается экран.  
  - Кнопка "+" открывает форму добавления задачи, перекрывая текущий экран.  
- **Взаимодействие с другими компонентами**:  
  - Связана с моделью данных через открытие формы добавления, которая взаимодействует с задачами.  
  - Переключает экраны (например, на настройки или список покупок).  
- **Состояния**:  
  - Активный/неактивный пункт навигации.  
  - Кнопка "+" всегда доступна, но может быть неактивна, если форма уже открыта.  
- **Особенности поведения**:  
  - Плавный переход между экранами с анимацией.  

#### Панель задач (Task Panel) — основной экран
Это центральный экран для управления задачами, включающий фильтры, список задач и взаимодействие с ними.

##### Верхняя панель фильтров
- **Назначение**: Позволяет пользователю фильтровать задачи по статусу (все, активные, выполненные) и приоритету (низкий, средний, высокий), а также искать по названию (в будущем).  
- **Визуальное состояние**:  
  - Компактные табы или выпадающие элементы для статуса и приоритета.  
  - Активный фильтр выделен цветом акцента (например, синий #007AFF).  
  - Поиск (если реализован) — текстовое поле с иконкой лупы, активное/неактивное состояние.  
- **Логика работы**:  
  - Выбор фильтра обновляет список задач, показывая только те, что соответствуют критериям.  
  - Например, выбор "Активные" показывает только незавершённые задачи, а "Высокий приоритет" — только красные задачи.  
- **Взаимодействие с другими компонентами**:  
  - Фильтры взаимодействуют с моделью данных, фильтруя задачи в списке.  
  - Применение фильтра обновляет `ListView`, отображая отфильтрованные карточки.  
- **Состояния**:  
  - По умолчанию: "Все" статус и без фильтра приоритета.  
  - Фильтрованные состояния, например, "Только активные" или "Только высокий приоритет".  
- **Особенности поведения**:  
  - Если фильтров нет, список показывает все задачи.  
  - Возможность сброса фильтров через тап на "Все".  

##### Список задач
- **Назначение**: Отображает задачи в виде карточек, позволяя пользователю просматривать, редактировать и управлять ими.  
- **Визуальное состояние**:  
  - Каждая задача — карточка высотой 72pt с тенью или границей.  
  - Слева цветовая полоса, соответствующая приоритету: зелёная (#34C759) для низкого, оранжевая (#FF9500) для среднего, красная (#FF4E4E) для высокого.  
  - Название задачи — крупным шрифтом (17pt, Medium), дедлайн — ниже (13pt, Regular, например, "Сегодня", "Завтра", "15 мая").  
  - При свайпе влево — карточка становится серой с иконкой корзины, вправо — с галочкой.  
- **Логика работы**:  
  - Список обновляется при изменении фильтров или добавлении/удалении задач.  
  - Если список пуст, показывает подсказку: "Нажмите +, чтобы добавить задачу".  
- **Взаимодействие с другими компонентами**:  
  - Связан с моделью данных для отображения задач.  
  - При тапе открывает форму редактирования через `Popup`.  
  - Свайп взаимодействует с моделью для удаления или отметки выполнения, а также с уведомлениями (snackbar).  
- **Состояния**:  
  - Пустой список: подсказка с призывом добавить задачу.  
  - Список с задачами: карточки с разными приоритетами и статусами.  
  - Завершённая задача: может быть зачёркнута или перемещена в отдельный раздел при фильтре "Готово".  
- **Особенности поведения**:  
  - Свайп влево удаляет задачу, показывая snackbar: "Задача удалена. Отменить?" с кнопкой "Отменить".  
  - Свайп вправо отмечает задачу как выполненную, с визуальной обратной связью (например, зачёркивание).  
  - Цветовая индикация приоритета помогает быстро оценить важность задачи.  

##### Поведение при нажатии на задачу
- **Назначение**: Открывает детальное представление для просмотра или редактирования задачи.  
- **Визуальное состояние**:  
  - Открывается модальное окно с полным описанием задачи, полями для редактирования.  
  - Активное состояние: поля заполнены данными задачи, кнопки "Редактировать", "Удалить" активны.  
- **Логика работы**:  
  - При тапе на карточку открывается форма с данными задачи (название, описание, дедлайн, приоритет).  
  - Пользователь может изменить поля и сохранить изменения.  
- **Взаимодействие с другими компонентами**:  
  - Форма взаимодействует с моделью данных для обновления задачи.  
  - Кнопка "Удалить" вызывает удаление через модель, с возможностью отмены через snackbar.  
- **Состояния**:  
  - Режим просмотра: только чтение.  
  - Режим редактирования: поля активны для ввода.  
- **Особенности поведения**:  
  - Плавная анимация появления формы.  
  - Валидация: название обязательно для сохранения.  

##### Свайп влево/вправо
- **Назначение**: Быстрое управление задачей без дополнительных кликов.  
- **Визуальное состояние**:  
  - При свайпе влево — карточка становится серой с иконкой корзины, появляется snackbar.  
  - При свайпе вправо — иконка галочки, задача зачёркивается.  
- **Логика работы**:  
  - Свайп влево удаляет задачу, показывая snackbar с "Отменить".  
  - Свайп вправо отмечает задачу как выполненную, обновляя модель.  
- **Взаимодействие с другими компонентами**:  
  - Свайп влево вызывает удаление через модель, snackbar связан с уведомлениями.  
  - Свайп вправо обновляет статус задачи в модели.  
- **Состояния**:  
  - Нормальное: карточка в списке.  
  - Свайп: временное состояние с иконкой действия.  
  - После удаления: snackbar активен для отмены.  
- **Особенности поведения**:  
  - Snackbar появляется внизу экрана, исчезает через 5 секунд или при тапе "Отменить".  
  - Отмена восстанавливает задачу в списке.  

##### Кнопка добавления задачи
- **Назначение**: Быстрое создание новой задачи.  
- **Визуальное состояние**:  
  - Круглая кнопка "+" в нижней навигации, акцентный цвет (#007AFF), активна всегда.  
  - При нажатии — выделяется (масштабирование или тень).  
- **Логика работы**:  
  - При тапе открывается форма добавления задачи в модальном окне.  
- **Взаимодействие с другими компонентами**:  
  - Открывает форму, связанную с моделью данных для добавления новой задачи.  
- **Состояния**:  
  - Активна, если форма не открыта.  
- **Особенности поведения**:  
  - Плавная анимация появления формы.  

#### Другие экраны

##### Список покупок
- **Назначение**: Отдельный раздел для управления списками покупок (пока заглушка).  
- **Визуальное состояние**:  
  - Пустой экран с текстом "Скоро" или "Функция в разработке".  
- **Логика работы**:  
  - Пока нет функционала, только информационное сообщение.  
- **Взаимодействие с другими компонентами**:  
  - Переключается через навигацию, без взаимодействия с моделью данных.  
- **Состояния**:  
  - Единственное состояние — пустой экран.  
- **Особенности поведения**:  
  - В будущем: чекбоксы, категории, синхронизация.  

##### Настройки
- **Назначение**: Настройка пользовательских параметров.  
- **Визуальное состояние**:  
  - Поля для ввода имени пользователя, возможные переключатели для темы (в будущем).  
  - Активное/неактивное состояние для полей.  
- **Логика работы**:  
  - Пользователь вводит имя, которое сохраняется в настройках.  
  - В будущем: выбор темы, уведомления, статистика.  
- **Взаимодействие с другими компонентами**:  
  - Имя пользователя может отображаться в шапке или профиле.  
  - Тема влияет на цветовую схему интерфейса.  
- **Состояния**:  
  - Пустое поле имени, заполненное поле.  
  - В будущем: светлая/тёмная тема, включённые/выключенные уведомления.  
- **Особенности поведения**:  
  - Автосохранение имени или кнопка "Сохранить".  

### Визуальное представление (как в Figma, сверху вниз)
📱 Экран: Панель задач (Task Panel)  
┌─────────────────────────────────────┐  
│  [ Фильтры ]                        │  
│  ● Все    ● Активные    ● Готово    │  
│  [Выпадающий список: Приоритет]     │  
├─────────────────────────────────────┤  
│  [Карточка задачи 1]                │  
│  ┌───────────────────────────────┐  │  
│  │ 🟢 Низкий приоритет           │  │  
│  │ Подготовить отчёт              │  │  
│  │ Дедлайн: Сегодня                │  │  
│  └───────────────────────────────┘  │  
│  [Карточка задачи 2]                │  
│  ┌───────────────────────────────┐  │  
│  │ 🟠 Средний приоритет          │  │  
│  │ Купить продукты                 │  │  
│  │ Дедлайн: Завтра                  │  │  
│  └───────────────────────────────┘  │  
│  [Карточка задачи 3]                │  
│  ┌───────────────────────────────┐  │  
│  │ 🔴 Высокий приоритет          │  │  
│  │ Встреча с клиентом               │  │  
│  │ Дедлайн: 15 мая                  │  │  
│  └───────────────────────────────┘  │  
│  [Пустое состояние]                 │  
│  "Нажмите +, чтобы добавить задачу" │  
└─────────────────────────────────────┘  
│  [ Навигационная панель ]           │  
│  📋 Задачи     🛒 Покупки     ➕     ⚙️ Настройки │  
└─────────────────────────────────────┘  

### Таблица: Состояния и взаимодействия компонентов

| Компонент          | Состояния                          | Взаимодействия                     | Особенности поведения               |
|--------------------|------------------------------------|------------------------------------|-------------------------------------|
| Навигационная панель | Активный/неактивный пункт, кнопка "+" | Тап для переключения экранов       | Плавный переход, акцент на активном |
| Фильтры            | Активный/неактивный фильтр         | Тап для фильтрации, выбор приоритета | Сброс фильтров через "Все"          |
| Карточка задачи    | Нормальная, свайп, завершённая     | Тап (редактирование), свайп (удаление/выполнение) | Цветовая индикация, snackbar для отмены |
| Форма задачи       | Добавление, редактирование         | Ввод данных, выбор приоритета, сохранение/отмена | Валидация названия, анимация появления |
| Snackbar           | Активное (удаление), неактивное    | Тап на "Отменить" для восстановления | Исчезает через 5 секунд             |

Этот функционал обеспечивает интуитивное управление задачами с заделом на будущие улучшения, такие как статистика, уведомления и синхронизация.





### Ответ на запрос

**Ключевые моменты**:
- Текущие наработки приложения TaskTracker включают функциональный экран задач с фильтрацией по приоритету, модальную форму добавления/редактирования задач и поддержку шрифтов Roboto, что хорошо соответствует новому плану разработки.
- Новый план требует добавления нижней навигационной панели с четырьмя пунктами (Задачи, Список покупок, Кнопка добавления, Настройки), свайп-действий для задач, цветовых индикаторов приоритета, уведомлений (snackbar) и дополнительных экранов.
- Интеграция текущих наработок с новым планом возможна и предпочтительна, так как существующие компоненты (`Header.qml`, `TaskItem.qml`, `AddTaskForm.qml`) уже реализуют значительную часть функционала.
- Начать с нуля менее эффективно, так как потребуется переписывать уже работающие элементы, такие как список задач и форма, которые можно адаптировать.

**Почему интеграция предпочтительна**  
Текущие QML-файлы (`main.qml`, `AddTaskForm.qml`, `TaskItem.qml`, `Header.qml`, `Style.qml`) обеспечивают базовую функциональность: список задач, фильтрация по приоритету, модальная форма с анимацией. Новый план добавляет нижнюю навигацию, свайп-действия, уведомления и дополнительные экраны, которые можно реализовать, модифицируя существующие компоненты. Это экономит время и сохраняет стабильность.

**Основные шаги интеграции**  
- **Нижняя навигация**: Добавить панель с кнопками для переключения между экранами (Задачи, Список покупок, Настройки) и центральной кнопкой добавления.
- **Экран задач**: Улучшить фильтры (добавить статус: все, активные, выполненные), внедрить свайп-действия и цветовые полосы приоритета.
- **Уведомления**: Реализовать snackbar для отмены удаления.
- **Дополнительные экраны**: Создать заглушки для списка покупок и настроек с базовой функциональностью (например, ввод имени пользователя).

**Когда стоит начать с нуля**  
Начать с нуля имеет смысл только при значительных архитектурных изменениях, например, если потребуется полностью новая структура данных или переход на другую версию Qt. Однако текущие наработки совместимы с планом, и их адаптация будет быстрее.

---

### Подробное описание интеграции текущих наработок с новым планом разработки

**Текущие наработки**  
Текущее приложение TaskTracker, реализованное в QML, включает следующие компоненты:  
- **main.qml**: Основной экран с заголовком, кнопкой добавления, фильтром приоритета (`ComboBox`) и списком задач (`ListView`). Форма добавления/редактирования открывается как модальный `Popup`.  
- **AddTaskForm.qml**: Модальная форма с полями для названия, описания, приоритета (кнопки вместо `ComboBox`), а также неактивными полями для категории и срока выполнения. Поддерживает анимации появления/исчезновения.  
- **TaskItem.qml**: Карточка задачи с названием, описанием, приоритетом и кнопкой удаления.  
- **Header.qml**: Простой заголовок с текстом "Task Tracker".  
- **Style.qml**: Синглтон для управления стилями (цвета, шрифты, отступы), поддерживающий светлую/тёмную тему.  

Шрифты Roboto (`Regular`, `Bold`, `ExtraBold`, `Light`) используются для разных элементов интерфейса, а фильтрация по приоритету реализована через `visible` в `ListView.delegate`.

**Новый план разработки**  
Новый план требует:  
- Нижнюю навигационную панель с четырьмя пунктами: Задачи, Список покупок, Кнопка добавления (+), Настройки.  
- Улучшенный экран задач с фильтрами по статусу (все, активные, выполненные) и приоритету, цветовой индикацией приоритета (зелёный, оранжевый, красный), свайп-действиями (удаление, выполнение) и уведомлениями (snackbar).  
- Заглушки для списка покупок и настроек с базовой функциональностью (например, ввод имени пользователя).  
- Минималистичный дизайн с акцентом на интуитивность, анимации и защиту от случайных действий (например, отмена удаления).  

**Оценка совместимости**  
Текущие наработки покрывают значительную часть требований:  
- Экран задач с фильтрацией по приоритету уже реализован.  
- Форма добавления/редактирования с анимацией и поддержкой будущих полей (категория, дедлайн) готова.  
- Шрифты и стили соответствуют предложенной палитре (например, `#007AFF` для акцентов, `#34C759` для низкого приоритета).  
- Отсутствуют: нижняя навигация, свайп-действия, уведомления, фильтры по статусу и дополнительные экраны.  

Интеграция возможна, так как текущие компоненты можно адаптировать, добавив недостающие элементы, вместо переписывания с нуля.

**План интеграции**  
Ниже описаны шаги для интеграции текущих наработок с новым планом, с акцентом на функциональность и взаимодействие.

#### 1. Нижняя навигационная панель
- **Назначение**: Обеспечивает переключение между экранами (Задачи, Список покупок, Настройки) и быстрый доступ к добавлению задачи.  
- **Интеграция**:  
  - В `main.qml` заменить текущую структуру `Column` на `StackLayout` для переключения экранов (Задачи, Список покупок, Настройки).  
  - Добавить `RowLayout` внизу с четырьмя элементами: кнопки для Задач, Списка покупок, центральная кнопка "+" (FAB) и Настройки.  
  - Кнопка "+" будет открывать `AddTaskForm.qml` как модальный `Popup`, как сейчас.  
- **Визуальное состояние**:  
  - Активный экран выделяется цветом (#007AFF) или подчёркиванием.  
  - Кнопка "+" — круглая, с акцентным цветом, с анимацией нажатия (масштабирование).  
- **Логика работы**:  
  - Тап на "Задачи" показывает текущий экран задач (`ListView` с фильтрами).  
  - Тап на "Список покупок" или "Настройки" переключает `StackLayout` на соответствующий экран.  
  - Тап на "+" открывает форму добавления.  
- **Взаимодействие**:  
  - Связывает экраны через индекс `StackLayout`.  
  - Кнопка "+" взаимодействует с `AddTaskForm.qml` для создания задачи.  
- **Состояния**:  
  - Активный/неактивный пункт навигации.  
  - Кнопка "+" всегда активна, но не выделяется как экран.  
- **Особенности**:  
  - Плавная анимация переключения экранов.  

#### 2. Экран задач (Task Panel)
- **Назначение**: Основной экран для просмотра, фильтрации и управления задачами.  
- **Интеграция**:  
  - Сохранить текущий `ListView` из `main.qml` с `TaskItem.qml` как делегатом.  
  - Добавить фильтры по статусу (все, активные, выполненные) через `Row` с кнопками над `ComboBox` для приоритета.  
  - Реализовать свайп-действия в `TaskItem.qml` с помощью `SwipeDelegate`.  
  - Добавить цветовую полосу слева в карточке задачи.  
  - Внедрить snackbar для отмены удаления.  
- **Верхняя панель фильтров**:  
  - **Назначение**: Фильтрация задач по статусу и приоритету.  
  - **Визуальное состояние**:  
    - Табы "Все", "Активные", "Готово" (шрифт 14pt, Semibold).  
    - `ComboBox` для приоритета (Все, Низкий, Средний, Высокий).  
    - Активный фильтр выделен синим (#007AFF).  
  - **Логика работы**:  
    - Тап на таб обновляет список задач, показывая только соответствующие (например, "Готово" — завершённые задачи).  
    - Выбор приоритета фильтрует задачи по значению (1-3).  
  - **Взаимодействие**:  
    - Фильтры обновляют `ListView` через свойство `visible` или будущую прокси-модель.  
  - **Состояния**:  
    - По умолчанию: "Все" и без фильтра приоритета.  
    - Фильтрованные состояния: "Активные", "Готово", "Высокий приоритет".  
  - **Особенности**:  
    - Сброс фильтров через "Все".  
- **Список задач**:  
  - **Назначение**: Отображение задач в виде карточек.  
  - **Визуальное состояние**:  
    - Карточка (72pt высотой, белый фон #FFFFFF, тень).  
    - Цветовая полоса слева: зелёная (#34C759), оранжевая (#FF9500), красная (#FF4E4E).  
    - Название (17pt, Medium), дедлайн (13pt, Regular), приоритет (текст или цвет).  
  - **Логика работы**:  
    - Тап открывает форму редактирования (`AddTaskForm.qml`).  
    - Свайп влево: удаление с показом snackbar.  
    - Свайп вправо: отметка как выполненная (зачёркивание).  
  - **Взаимодействие**:  
    - Связан с моделью данных (`taskModel`) для отображения и обновления.  
    - Snackbar взаимодействует с моделью для отмены удаления.  
  - **Состояния**:  
    - Пустой список: подсказка "Нажмите +, чтобы добавить задачу".  
    - Завершённая задача: зачёркнута или скрыта при фильтре "Активные".  
  - **Особенности**:  
    - Цветовая полоса для быстрого восприятия приоритета.  
    - Snackbar появляется на 5 секунд с кнопкой "Отменить".  
- **Кнопка добавления**:  
  - **Назначение**: Открытие формы для создания задачи.  
  - **Визуальное состояние**:  
    - Круглая кнопка "+" в навигации, синий цвет (#007AFF).  
  - **Логика работы**:  
    - Открывает `AddTaskForm.qml` как модальный `Popup`.  
  - **Взаимодействие**:  
    - Связана с моделью для добавления задачи.  
  - **Состояния**:  
    - Активна, если форма не открыта.  
  - **Особенности**:  
    - Анимация нажатия (масштабирование).  

#### 3. Форма добавления/редактирования задачи
- **Назначение**: Создание или редактирование задачи.  
- **Интеграция**:  
  - Сохранить текущий `AddTaskForm.qml` с `Popup`, активировать поля `category` и `dueDate`.  
  - Добавить календарь для выбора даты (например, через `Calendar`).  
- **Визуальное состояние**:  
  - Модальное окно с полями: название, описание (`TextArea`), приоритет (кнопки), категория, дедлайн.  
  - Кнопки "Добавить"/"Сохранить" (#007AFF) и "Отмена" (#E0E0E0).  
- **Логика работы**:  
  - Валидация: название обязательно.  
  - Сохранение обновляет модель.  
- **Взаимодействие**:  
  - Связана с `taskModel` для добавления/обновления.  
- **Состояния**:  
  - Режим добавления: пустые поля.  
  - Режим редактирования: заполненные поля.  
- **Особенности**:  
  - Плавная анимация появления.  

#### 4. Список покупок
- **Назначение**: Заглушка для будущего функционала.  
- **Интеграция**:  
  - Создать новый QML-файл (`ShoppingList.qml`) с текстом "Скоро".  
  - Добавить в `StackLayout` как второй экран.  
- **Визуальное состояние**:  
  - Текст "Скоро" (15pt, Regular).  
- **Логика работы**:  
  - Пустой экран, доступный через навигацию.  
- **Взаимодействие**:  
  - Переключается через `StackLayout`.  
- **Состояния**:  
  - Единственное состояние: заглушка.  
- **Особенности**:  
  - Задел для чекбоксов и категорий.  

#### 5. Настройки
- **Назначение**: Ввод имени пользователя и задел для тем, уведомлений, статистики.  
- **Интеграция**:  
  - Создать `Settings.qml` с полем для имени пользователя.  
  - Добавить в `StackLayout` как третий экран.  
- **Визуальное состояние**:  
  - Поле ввода имени (16pt, Regular).  
- **Логика работы**:  
  - Сохранение имени в настройках.  
- **Взаимодействие**:  
  - Имя может отображаться в заголовке.  
- **Состояния**:  
  - Пустое/заполненное поле имени.  
- **Особенности**:  
  - Задел для переключения тем.  

#### 6. Snackbar
- **Назначение**: Уведомление об удалении с возможностью отмены.  
- **Интеграция**:  
  - Создать `SnackBar.qml` как `Popup`, появляющийся внизу экрана.  
  - Активировать при свайпе влево в `TaskItem.qml`.  
- **Визуальное состояние**:  
  - Текст "Задача удалена. Отменить?" с кнопкой.  
- **Логика работы**:  
  - Появляется на 5 секунд, тап на "Отменить" восстанавливает задачу.  
- **Взаимодействие**:  
  - Связан с моделью для восстановления задачи.  
- **Состояния**:  
  - Активное (после удаления), неактивное.  
- **Особенности**:  
  - Автоматическое исчезновение.  

**Таблица: Сравнение текущих наработок и нового плана**

| Элемент                  | Текущие наработки                              | Новый план                                   | Действия для интеграции                     |
|--------------------------|-----------------------------------------------|---------------------------------------------|---------------------------------------------|
| Навигация                | Отсутствует                                   | Нижняя панель с 4 пунктами                  | Добавить `RowLayout` с кнопками             |
| Экран задач              | `ListView` с фильтром приоритета              | Фильтры по статусу, свайпы, цветовые полосы | Добавить фильтры, `SwipeDelegate`, полосы   |
| Форма задачи             | `Popup` с полями, анимацией                   | Добавить календарь, активировать поля        | Активировать `category`, `dueDate`, календарь |
| Список покупок           | Отсутствует                                   | Заглушка "Скоро"                            | Создать `ShoppingList.qml`                  |
| Настройки                | Отсутствует                                   | Поле имени, задел для тем                   | Создать `Settings.qml`                      |
| Snackbar                 | Отсутствует                                   | Уведомление с отменой                       | Создать `SnackBar.qml`                      |

**Почему не начинать с нуля**  
- Текущие компоненты (`TaskItem.qml`, `AddTaskForm.qml`) уже реализуют ключевые функции (список, добавление/редактирование).  
- Стили (`Style.qml`) соответствуют новой палитре (#007AFF, #34C759 и т.д.).  
- Переписывание потребует повторной реализации работающих частей, что увеличит время разработки.  

**Рекомендации**  
Интеграция предпочтительна, так как позволяет использовать существующие компоненты, добавляя недостающие элементы (навигация, свайпы, snackbar). Если потребуется значительное изменение модели данных (например, добавление статуса), это можно сделать позже без переписывания UI.

**Цитаты**:
- [Qt Documentation: Navigation Controls](https://doc.qt.io/qt-5/qtquickcontrols2-navigation.html)
- [Google Material Design: Bottom Navigation](https://material.google.com/components/bottom-navigation.html)

